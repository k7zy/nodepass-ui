version: '3.8'

services:
  # NodePass 应用 - 使用GitHub发布的镜像
  app:
    image: ghcr.io/mecozea/nodepass-webui:latest
    container_name: nodepass-app
    environment:
      # 应用配置(修改这里)
      - JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
    ports:
      - "3000:3000"  # 整合的Next.js应用
    volumes:
      # 挂载配置和日志
      - ./config:/app/config:ro
      # SQLite 数据库文件
      - ./public:/app/public
      # 日志和数据目录,如需挂载，请先赋予文件夹权限```chmod 777 logs```，否则可能会导致容器启动失败
      # - ./logs:/app/logs
      # - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

# 🚀 使用说明:
# 1. 创建 .env 文件并设置环境变量
# 2. 运行: docker-compose -f docker-compose.release.yml up -d
# 3. 访问: http://localhost:3000
#
# 📝 环境变量示例:
# JWT_SECRET=your_super_secret_jwt_key
# NEXT_PUBLIC_API_BASE_URL=http://your-domain.com:3000  # 云端部署时修改为实际域名 